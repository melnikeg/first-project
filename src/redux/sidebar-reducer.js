let initialState = {
    friends: [
        { name: 'Zhenya', id: 1 },
        { name: 'Anton', id: 2 },
        { name: 'Andrew', id: 3 }
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return(state);
}

export default sidebarReducer;